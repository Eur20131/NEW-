{
  "name": "mattcup-roastery",
  "version": "1.0.0",
  "description": "MattCup Roastery website with Vercel Analytics",
  "private": true,
  "scripts": {
    "build": "echo 'Static HTML build'",
    "dev": "npx serve .",
    "lint": "echo 'No linter configured'"
  },
  "dependencies": {
    "@vercel/analytics": "^1.4.1"
  }
}
